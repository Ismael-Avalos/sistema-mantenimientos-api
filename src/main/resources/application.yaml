spring:
  application:
    name: mantenimientos
  datasource:
    url: jdbc:postgresql://localhost:5432/sistema_mantenimientos_bd
    username: postgres
    password: "ismael2026"
    driver-class-name: org.postgresql.Driver
  jpa:
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    hibernate:
      ddl-auto: validate
  flyway:
    baseline-on-migrate: true
  security:
    user:
      name: ismael2026
      password: isma12345