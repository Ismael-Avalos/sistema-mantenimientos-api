spring:
  application:
    name: mantenimientos
  datasource:
    url: ${DB_URL}
    username: ${DB_USERAPI}
    password: ${DB_PASSWORDAPI}
    driver-class-name: org.postgresql.Driver
  jpa:
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    hibernate:
      ddl-auto: validate
  flyway:
    baseline-on-migrate: true
  security:
    user:
      name: admin
      password: isma12345